# Let's Encrypt Certificates

This directory is intentionally not tracked in git (the `.pem` files are large and
reproducible).

To download the current Let's Encrypt roots and intermediates for local testing:

```bash
make download-letsencrypt
```

